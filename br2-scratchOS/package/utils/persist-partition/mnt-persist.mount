[Unit]
Description=Partition for Persistent Data /mnt/persist
Wants=tmp.mount
Before=local-fs.target umount.target

[Mount]
What=/dev/mmcblk0p4
Where=/mnt/persist
Type=ext4

[Install]
WantedBy=local-fs.target
